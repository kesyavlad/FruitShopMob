import React from 'react';
import {SafeAreaView, Text} from 'react-native';

const CardShop = () => {
  return (
    <SafeAreaView>
      <Text>Apple</Text>
    </SafeAreaView>
  );
};

export default CardShop;
